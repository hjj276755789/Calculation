@using Calculation.Base;
@using Calculation.Models;
@{
    ViewBag.Title = "Index";
}

    @{
        var qxxx = this.ViewBag.nav as List<QXXX>;
        if (qxxx != null && qxxx.Count > 0)
        {
            foreach (var item in qxxx)
            {
                string path = "/" + item.qxkzq + "/" + item.qxst;

                    <div class="col-md-6 col-sm-6 col-lg-3">
                        <a  href="@path" >
                            <div class="mini-stat clearfix bg-purple bx-shadow">
                                <span class="mini-stat-icon"><i class="ion-ios7-cart"></i></span>
                                <div class="tiles-progress">
                                    <div class="m-t-20">
                                        <h5 class="text-uppercase text-white m-0">@item.qxmc <span class="pull-right"></span></h5>
                                    </div>
                                </div>
                            </div>
                        </a>
                    </div>
            }
        }
    }


@section scripts{
<script src="~/Scripts/Moltran/modal/classie.js"></script>
<script src="~/Scripts/Moltran/modal/modalEffects.js"></script>
}